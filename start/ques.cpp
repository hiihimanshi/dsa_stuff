#include <iostream>
using namespace std;
int main()
{
    int n;
    cin n;
    int amt = n;
    int ans;

    for (i = 1; i <= 3; i++)
    {
        switch (i)
        {
        case 1:
            int rem = amt / 100;
            cout << "100" << rem << endl;
            ans = amt - rem;
            break;

        case 2:
            cout < < < < endl;
            break;

        case 3:
            cout < < < < endl;
            break;

        default:
            break;
        }
    }

    return 0;
}